const Post = require("../models/Post");

const PostController = {
  async create(req, res) {
    try {
    } catch (error) {
      console.error(error);
    }
  },
};

module.exports = PostController;
